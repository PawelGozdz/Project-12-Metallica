import '../scss/main.scss';
import 'bootstrap';
// import 'bootstrap/js/dist/util';
// import 'bootstrap/js/dist/dropdown';
// import '../css/style.css';
import num from './test';

const x = 2333;
console.log(`I imported ${num} from another module with number ${x}`);
console.log(`I imported ${num} from another module with number ${x}`);
console.log(`I imported ${num} from another module with number ${x}`);
console.log(`I imported ${num} from another module with number ${x}`);
console.log(`I imported ${num} from another module with number ${x}`);
console.log(`I imported ${num} from another module with number ${x}`);
console.log(`I imported ${num} from another module with number ${x}`);
